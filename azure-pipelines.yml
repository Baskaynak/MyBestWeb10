# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main
- feature/nazar-boncugu-v2

pool:
  vmImage: 'ubuntu-latest'

variables:
  buildConfiguration: 'Release'

steps:
# 1. .NET 10 SDK Kurulumu
- task: UseDotNet@2
  displayName: 'Install .NET 10 SDK (Preview)'
  inputs:
    packageType: 'sdk'
    version: '10.x'
    includePreviewVersions: true

# 2. Restore
- task: DotNetCoreCLI@2
  displayName: 'Restore'
  inputs:
    command: 'restore'
    projects: 'MyBestWeb10/MyBestWeb10.csproj'

# 3. Build
- task: DotNetCoreCLI@2
  displayName: 'Build'
  inputs:
    command: 'build'
    projects: 'MyBestWeb10/MyBestWeb10.csproj'
    arguments: '--configuration $(buildConfiguration)'

# 4. Publish (Otomatik olarak MyBestWeb10.zip oluşturur)
- task: DotNetCoreCLI@2
  displayName: 'Publish'
  inputs:
    command: 'publish'
    publishWebProjects: false
    projects: 'MyBestWeb10/MyBestWeb10.csproj'
    arguments: '--configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)'
    zipAfterPublish: true

# 5. Azure Deployment (Tek ve Net Yol)
- task: AzureRmWebAppDeployment@4
  displayName: 'Deploy to Azure'
  inputs:
    ConnectionType: 'AzureRM'
    azureSubscription: 'MyAzureConn'
    appType: 'webAppLinux'
    WebAppName: 'BasicWeb'
    # Doğrudan spesifik zip dosyasına odaklanıyoruz:
    packageForLinux: '$(Build.ArtifactStagingDirectory)/MyBestWeb10.zip'